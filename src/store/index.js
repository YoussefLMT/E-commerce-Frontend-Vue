import { createStore } from 'vuex'
import AuthModule from './AuthModule'

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    auth: AuthModule,
  }
})
